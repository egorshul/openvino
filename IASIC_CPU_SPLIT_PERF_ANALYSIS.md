# Анализ производительности: IASIC + CPU (pre/post) split

Разбор, почему сплит-пайплайн (`pre → IASIC → post`) даёт просадку, хотя CPU-части
тривиальны. Вывод: дело **не в CPU-операциях и не в сборке OpenVINO**, а в
**хостовой драйверной обвязке вокруг IASIC** (передачи + submit/wait).

---

## 1. Контекст

- Большая сеть исполняется на **IASIC** (основная часть) + **CPU** (pre и post).
- `pre` = один `Transpose` u8 `[1,3,112,112] → [1,112,112,3]`.
- `post` = `Slice / Reshape / TopK([1,5]) / Convert / Ceiling` на тензорах ≤128 элементов.
- Наблюдение: на одной модели просадка ~2×, на другой ~0%, на третьей ~20%.

---

## 2. CPU-части (pre/post) измерены — они ничтожны

`benchmark_app -m {pre,post}.xml -d CPU -hint latency`:

| стадия | latency (median) |
|--------|------------------|
| `pre` (Transpose) | **0.05 ms** |
| `post` (Slice/TopK/…) | **0.11 ms** |
| **pre + post** | **≈ 0.16 ms** |

Доля в полном инференсе (~6.5 ms): **2–5 %**. То есть CPU-операции физически не
могут давать 2× — интуиция «на CPU нет тяжёлых операций» подтверждена числами.

> Нюанс: `pre` быстрее в мультипотоке (0.05 мс), чем в 1 поток (0.26 мс) — Transpose
> параллелится. Форсить 1 поток для pre не нужно; оставляйте дефолтный latency-hint.

---

## 3. Профиль одного инференса (отдельная программа, не benchmark_app)

Единичный инференс, синхронно, без перекрытия. Числа в наносекундах → переведены в мс.

| метрика | время | смысл |
|---------|-------|-------|
| **Host.Inference.Total** | **6.48 ms** | весь инференс с трансферами |
| Transfer.Inputs | 1.62 ms | запись входа на девайс |
| Transfer.Outputs | 2.09 ms | выкачка выходов |
| Host.Inference.Inference | 2.11 ms | хост ждёт девайс (busy-wait) |
| Device.Inference.Total | 1.96 ms | сторона «демона» |
| **Device.Inference.Inference** | **1.75 ms** | **реальный компьют ускорителя** |

Детально по передачам (ключевое — `dmaTime` ничтожен):

| | host.total | serverTotalTime (демон) | **dmaTime (реальный DMA)** |
|---|---|---|---|
| WR вход (150 КБ) | 1.52 ms | 0.57 ms | **0.045 ms** |
| RD выходы (4 чтения × 512 Б) | 2.03 ms | 1.36 ms | **0.10 ms** |

---

## 4. Главный вывод

1. **Реальный компьют IASIC — 1.75 мс.** Остальное (6.48 − 1.75 = **4.7 мс ≈ 73 %**) —
   **оверхед**, а не вычисления.
2. **Передачи стоят 3.7 мс на хосте, при этом сам DMA — всего ~0.15 мс.**
   «Трансфер» — это **не движение данных**, а **хостовая/демонская обвязка**:
   - вход: 1.52 мс хоста при DMA 45 **микро**секунд (×33);
   - выходы: 2.03 мс хоста при DMA 0.1 мс (×20).
3. **Выходы читаются 4 отдельными раунд-трипами** (4 memhandle по 512 Б),
   каждый ~0.5 мс хоста → ~2 мс почти на пустом месте.

«CPU ничего не считает» — верно по **OV-слоям** (0.16 мс). Но хост **ест на
драйверной обвязке IASIC**: submit/wait + фикс-косты на каждый memhandle +
копии/syscalls. Это **однопоточный** путь.

---

## 5. Почему это и есть x86 ↔ ARM 2×

- `Device.Inference.Inference ≈ 1.72–1.75 мс` **одинаков на x86 и ARM** — это
  **только ускоритель**, железо одно и то же.
- **Host-часть** (3.7 мс передач + 2.1 мс busy-wait) исполняется на **хостовом ядре**:
  - почти не зависит от DMA (DMA = 0.15 мс на обеих платформах);
  - упирается в **скорость одного ядра хоста**;
  - ядро **Cortex-A75 @ 2 ГГц в разы слабее Xeon** на memcpy / syscalls / submit /
    busy-wait → та же обвязка на ARM в ~2× дороже.

Отсюда же и «разные просадки на разных моделях»: оверхед `O` почти константа, а
компьют IASIC `T_main` разный, поэтому

```
относительная просадка ≈ O / T_main
```

- лёгкая модель (малое `T_main`) → 2×;
- тяжёлая (большое `T_main`) → ~0%;
- средняя → ~20%.

benchmark_app не показывает `O` как «CPU-слой» — это драйверный путь плагина.

---

## 6. Что чинить (всё на host-стороне, по убыванию эффекта)

1. **Слить 4 выходных чтения в одно.** 4 раунд-трипа × ~0.5 мс ≈ **2 мс** на ровном
   месте. Один общий memhandle уберёт ~1.5 мс.
2. **Zero-copy / pinned / shared-буферы.** `host.total ≫ dmaTime` означает, что хост
   **копирует и обслуживает буферы**, а не двигает данные. Маппинг общей памяти
   убирает ~1.5 мс на входе и большую часть на выходе.
3. **Async / конвейеризация.** `Host.Inference.Inference = 2.11 мс` — это **busy-wait**
   девайса. Перекрыть ожидание/передачи соседними запросами (несколько запросов в
   полёте) — спрячет часть оверхеда.
4. **Меньше раунд-трипов в целом** — батч-submit, если SDK позволяет.

---

## 7. Итог

| компонент | время | где исполняется | зависит от хоста? |
|-----------|-------|-----------------|-------------------|
| Компьют IASIC | 1.75 ms | ускоритель | нет (равен x86/ARM) |
| **Convert u8→f32 в pre** | **1.63 ms (ARM) / 0.08 (x86)** | хостовый CPU | **да** (A75 ≪ Xeon, ×20) |
| Передачи (host overhead) | ~3.5 ms | хостовый CPU | **да** (A75 ≪ Xeon) |
| Busy-wait девайса | ~2.1 ms | хостовый CPU | **да** |
| post (OV) + Transpose | ~0.27 ms | хостовый CPU | незначимо |
| реальный DMA | ~0.15 ms | железо | нет |

> Уточнение к разд. 2: `benchmark_app` по `pre.xml` показывал лишь Transpose
> (0.05 мс) — отдельный **`Convert` u8→f32** живёт в реальном split-пайплайне и в
> бенче не замерялся. На ARM он стоит **1.63 мс** (см. разд. 8a) — это и есть
> «вторая половина» разрыва ARM↔x86.

- 2× ARM↔x86 = **хостовый драйверный оверхед**, на котором A75 в разы медленнее Xeon.
- Сборка OpenVINO и pre/post тут **ни при чём**.
- Быстрый выигрыш: **объединить выходные чтения** и **zero-copy буферы** — это бьёт
  ровно в те ~3.5 мс, что сейчас съедает хост.

---

## 8. Прямое сравнение x86 vs ARM (измерено)

Один и тот же `_delta`-билд, одна модель, один инференс. Числа в мс.

| метрика | ARM (Baikal A75) | x86 (Xeon 6444Y) | разница |
|---|---|---|---|
| **Host.Inference.Total** | **6.48** | **4.45** | **+2.03 (ARM)** |
| Transfer.Inputs (запись входа) | 1.62 | 1.10 | +0.52 |
| **Transfer.Outputs (чтение выходов)** | **2.09** | **0.45** | **+1.64** ← главное |
| Host.Inference.Inference (busy-wait) | 2.11 | 2.29 | −0.18 |
| **Device.Inference.Inference (компьют)** | **1.75** | **1.74** | **≈0** |

Детально по 4 выходным чтениям (по 512 байт каждое):

| | host.total / чтение | serverTotalTime (демон) | dmaTime (железо) |
|---|---|---|---|
| **ARM** | ~0.50 мс | ~0.34 мс | ~0.025 мс |
| **x86** | ~0.11 мс | ~0.074 мс | ~0.016 мс |
| отношение | **×4.5** | **×4.6** | ×1.5 |

**Вывод:** компьют IASIC одинаков (1.74 мс). ~80% разрыва (1.64 из 2.03 мс) — в
**чтении выходов**: 4 отдельных раунд-трипа, и обработка каждого на хосте/демоне
на ARM в **~4.5× медленнее** (DMA при этом одинаков). Это однопоточный драйверный
путь, упирающийся в одно ядро: A75 ≪ Xeon.

---

## 8a. Полный клиентский прогон ARM: откуда **весь** разрыв ~4 мс

Раздел 8 сравнивал **только subgraph1** (IASIC + его трансферы). Но полный
клиентский замер показал **бóльший** разрыв, чем эти 2 мс:

```
ARM  клиент total = 9.518 мс
x86  клиент total = 5.536 мс
разница           = +3.98 мс
```

Если смотреть только на `Transfer.Outputs` (+1.64 мс) или на subgraph1 целиком
(+2.0 мс) — **недосчитываешься ещё ~2 мс**. Они спрятаны в **другой стадии (pre)**.

Полная раскладка по субграфам:

| стадия | что | ARM | x86 | разница |
|---|---|---|---|---|
| subgraph0 (pre) | **Convert_36468** (u8→f32, вход 112×112×3) | **1.634** | ~0.08 | **+1.55** |
| subgraph0 (pre) | Transpose | 0.056 | (учтён выше) | ~0 |
| subgraph1 | Transfer.Inputs | 1.621 | 1.10 | +0.52 |
| subgraph1 | **Transfer.Outputs** | **2.093** | 0.45 | **+1.64** |
| subgraph1 | Host.Inference.Inference (busy-wait) | 2.105 | 2.29 | −0.18 |
| subgraph1 | **Device.Inference.Inference (компьют)** | **1.748** | **1.736** | **≈0** |
| subgraph2 (post) | мелкие OV-операции | ~0.21 | ~0.2 | ~0 |
| **итого (клиент)** | | **9.518** | **5.536** | **+3.98** |

**Разрыв ~4 мс = две почти равные половины по ~2 мс:**

```
~4 мс
├── ~1.6 мс  ← Convert_36468 в PRE (u8→f32):  ARM 1.634  vs  x86 ~0.08  (×20)
└── ~2.0 мс  ← host-трансферы subgraph1:        Transfer.Outputs +1.64, Transfer.Inputs +0.52
```

То есть `Transfer.Outputs` — **только одна половина**. Вторая половина —
**`Convert` на pre-стадии**: тот же общий OV-Convert, который на Xeon
векторизуется широко (~0.08 мс), на одном ядре A75 стоит 1.634 мс (×20).

Корректность этого прогона: ARM `1839=51`, CPU-референс тоже `51` — совпадает;
max|f16−f32| по эмбеддингу ~0.009 (нормальное f16-округление).

**Приоритет фиксов с учётом pre-Convert (по убыванию эффекта):**

1. **Убрать/удешевить `Convert` u8→f32 в pre (−~1.5 мс).** Подавать вход в u8 и
   конвертировать на устройстве; слить Convert+Transpose в одну проходку; либо
   делать конверт в NHWC одним NEON-циклом, а не общим OV-Convert. Самая крупная
   и дешёвая победа — её не видно в subgraph1.
2. **Слить 4 выходных чтения в одно (−~1.5 мс из 2.0).**
3. **Zero-copy / shared-буферы** (`host.total ≫ dmaTime`).
4. **Async / конвейеризация** — спрятать busy-wait (2.1 мс).

---

## 9. Простыми словами (для всех)

Представьте, что ускоритель (IASIC) — это очень быстрый цех, а хостовый процессор
(CPU) — это **кладовщик**, который носит детали в цех и забирает готовое.

- **Сам цех работает одинаково быстро** и на ARM, и на x86 — деталь делается за
  ~1.7 мс. Ускоритель один и тот же.
- Но вокруг цеха есть **возня кладовщика**: упаковать вход, отнести, подождать,
  забрать **четыре** готовые детали по отдельности. Эта возня — почти **три четверти**
  всего времени.
- На x86 кладовщик быстрый (мощное ядро Xeon). На ARM кладовщик медленный (ядро
  Cortex-A75 в ~5 раз слабее на такой «рутине»: копирование, системные вызовы,
  ожидание). Поэтому **одна и та же возня на ARM занимает в разы больше**.
- Особенно дорого — **ходить за выходами 4 раза подряд**. Каждый «поход» на ARM
  стоит ~0.5 мс, а их четыре → ~2 мс только на это.
- И ещё столько же (~1.6 мс) кладовщик тратит **на самом входе** — переводит
  картинку из u8 в float (`Convert`). На Xeon это мгновенно (~0.08 мс), а на слабом
  ARM-ядре — 1.6 мс. Это **вторая половина** разрыва, которую легко не заметить:
  она в стадии pre, а не в чтении выходов.

**Почему важно:** вы думали, что тормозят операции на CPU (Transpose, TopK и т.п.).
Но они почти ничего не весят (~0.2 мс). Тормозят **подготовка входа (Convert) и
доставка данных туда-обратно** — и именно они на слабом ARM-ядре дорогие.

**Что делать:** (1) удешевить **Convert u8→float на входе** (подавать u8 и
конвертировать на устройстве / одним NEON-проходом) — это −~1.5 мс; (2) заставить
кладовщика ходить за выходами **один раз вместо четырёх** (объединить 4 чтения в
одно) — ещё −~1.5 мс; (3) **не копировать буферы зря** (общая память / zero-copy).
Это уберёт основную часть лишнего времени и почти сравняет ARM с x86 — без всякой
переборки модели или пересборки OpenVINO.

---

## Методика измерения (для повторения)

1. `benchmark_app -m {pre,post}.xml -d CPU -hint latency -t 15` → `T_pre`, `T_post`.
2. Профиль реального инференса (SDK): `Host.Inference.*`, `Device.Inference.*`,
   per-memhandle `host.total / serverTotalTime / dmaTime`.
3. Раскладка: `overhead = Host.Inference.Total − Device.Inference.Inference`;
   `transfer_host_overhead = Transfer.* − dmaTime`.
4. Свип `nireq` (1/2/4/8) — проверка запаса конвейеризации.
5. Формула `просадка ≈ O / T_main` по трём моделям.
